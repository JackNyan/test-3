name: indicator_entry_sender
description: A form app to submit indicator entries.
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.14.0
  intl: ^0.18.0
