# Indicator Entry Sender

A Flutter app to input and send indicator data to a remote API. Originally built with PowerShell GUI, now available as a mobile app.

## Features
- Area selection
- Value input
- Date and UTC DateTime pickers
- Shift and leader info
- Submit to API

## Getting Started

Install Flutter, clone this repo, then run:

```bash
flutter pub get
flutter run
```
